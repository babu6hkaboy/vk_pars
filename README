Запуск: python run_vk_pars.py

Что за что отвечает:

1. generate_group_id - создает список ID групп VK для обработки.
2. get_groups_by_ids - отправляет запрос в VK для получения информации о группах.
3. save_to_json - сохраняет данные о группах в JSON-файл (создаёт его если нет).
4. main - генерит ID групп, отправляет реквест и сохраняет в файл.
